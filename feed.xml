<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ahmedrafid023.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ahmedrafid023.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-11T05:52:04+00:00</updated><id>https://ahmedrafid023.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">A Comprehensive Guide to Backpropagation</title><link href="https://ahmedrafid023.github.io/blog/2026/backpropagation/" rel="alternate" type="text/html" title="A Comprehensive Guide to Backpropagation"/><published>2026-01-08T00:00:00+00:00</published><updated>2026-01-08T00:00:00+00:00</updated><id>https://ahmedrafid023.github.io/blog/2026/backpropagation</id><content type="html" xml:base="https://ahmedrafid023.github.io/blog/2026/backpropagation/"><![CDATA[<p>This note provides a comprehensive and self-contained overview of Backpropagation, the fundamental algorithm used to train multi-layer artificial neural networks. The mathematical mechanics of the algorithm are derived in detail using the chain rule of calculus to compute gradients of the loss function with respect to each trainable parameter. The exposition is structured around two main phases: the forward pass, where input data is transformed into predictions, and the backward pass, where error gradients are propagated from the output layer back through the network. A complete derivation is presented for a two-layer neural network with ReLU activation, including a worked numerical example to illustrate the full training step.</p> <p><br/></p> <h2 id="backpropagation-in-a-two-layer-neural-network-with-relu">Backpropagation in a Two-Layer Neural Network with ReLU</h2> <p>Backpropagation is the core algorithm used to train neural networks. It efficiently computes gradients of the loss function with respect to all model parameters by recursively applying the chain rule of calculus. These gradients are then used to update the parameters using gradient-based optimization methods such as gradient descent. This section presents a complete derivation of backpropagation for a simple two-layer neural network with a ReLU activation function.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_images/backprop-480.webp 480w,/assets/img/post_images/backprop-800.webp 800w,/assets/img/post_images/backprop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_images/backprop.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: Architecture of the two-layer neural network used in this derivation, illustrating the forward pass (blue arrows) and backpropagation of errors (red dashed arrows).</figcaption> </figure> <p>The network architecture is: \(\text{Input } (3) \;\rightarrow\; \text{Hidden } (5) \;\rightarrow\; \text{Output } (3)\)</p> <p><br/></p> <h3 id="network-architecture">Network Architecture</h3> <p>Let the input vector be: \(x \in \mathbb{R}^{3}\)</p> <p>The first linear layer is defined as: \(z = W_1 x + b_1, \quad W_1 \in \mathbb{R}^{5 \times 3}, \; b_1 \in \mathbb{R}^{5}\)</p> <p>A ReLU activation is applied: \(h = \text{ReLU}(z)\)</p> <p>The second linear layer is: \(y = W_2 h + b_2, \quad W_2 \in \mathbb{R}^{3 \times 5}, \; b_2 \in \mathbb{R}^{3}\)</p> <p><br/></p> <h3 id="relu-activation">ReLU Activation</h3> <p>The Rectified Linear Unit (ReLU) is defined element-wise as: \(\text{ReLU}(z_i) = \max(0, z_i)\)</p> <p>Its derivative is:</p> \[\frac{d}{dz_i} \text{ReLU}(z_i) = \begin{cases} 1 &amp; \text{if } z_i &gt; 0 \\ 0 &amp; \text{if } z_i \le 0 \end{cases}\] <p><br/></p> <hr/> <h2 id="forward-pass-example">Forward Pass Example</h2> <h3 id="define-the-input">Define the Input</h3> <p>Let the input vector be: \(x = \begin{bmatrix} 1 \\ 2 \\ 3 \end{bmatrix}\) <strong>Shape:</strong> $(3 \times 1)$</p> <h3 id="first-linear-layer-3-to-5">First Linear Layer ($3 \to 5$)</h3> <h4 id="weights-and-bias">Weights and Bias</h4> <p>Weight matrix $W_1$ ($5 \times 3$): \(W_1 = \begin{bmatrix} 1 &amp; 0 &amp; 2 \\ 0 &amp; 1 &amp; 1 \\ 1 &amp; 1 &amp; 0 \\ 2 &amp; 0 &amp; 1 \\ 0 &amp; 2 &amp; 1 \end{bmatrix}\)</p> <p>Bias $b_1$ ($5 \times 1$): \(b_1 = \begin{bmatrix} 1 \\ 0 \\ -1 \\ 2 \\ 1 \end{bmatrix}\)</p> <h4 id="linear-transformation">Linear Transformation</h4> <p>First, we calculate the linear pre-activation output $z = W_1 x + b_1$.</p> <p>Compute the matrix-vector product $W_1 x$:</p> \[W_1 x = \begin{bmatrix} 1 &amp; 0 &amp; 2 \\ 0 &amp; 1 &amp; 1 \\ 1 &amp; 1 &amp; 0 \\ 2 &amp; 0 &amp; 1 \\ 0 &amp; 2 &amp; 1 \end{bmatrix} \begin{bmatrix} 1 \\ 2 \\ 3 \end{bmatrix} = \begin{bmatrix} 7 \\ 5 \\ 3 \\ 5 \\ 7 \end{bmatrix}\] <p>Add the bias vector $b_1$:</p> \[z = \begin{bmatrix} 7 \\ 5 \\ 3 \\ 5 \\ 7 \end{bmatrix} + \begin{bmatrix} 1 \\ 0 \\ -1 \\ 2 \\ 1 \end{bmatrix} = \begin{bmatrix} 8 \\ 5 \\ 2 \\ 7 \\ 8 \end{bmatrix}\] <p><br/></p> <h4 id="relu-activation-1">ReLU Activation</h4> <p>Now apply the Rectified Linear Unit (ReLU) activation function element-wise to get the hidden state $h$:</p> \[h = \text{ReLU}(z) = \max(0, z)\] <p>Since all elements in $z$ are positive, the values remain unchanged:</p> \[h = \text{ReLU} \left( \begin{bmatrix} 8 \\ 5 \\ 2 \\ 7 \\ 8 \end{bmatrix} \right) = \begin{bmatrix} 8 \\ 5 \\ 2 \\ 7 \\ 8 \end{bmatrix}\] <p><strong>Shape:</strong> $(5 \times 1)$</p> <h3 id="second-linear-layer-5-to-3">Second Linear Layer ($5 \to 3$)</h3> <h4 id="weights-and-bias-1">Weights and Bias</h4> <p>Weight matrix $W_2$ ($3 \times 5$): \(W_2 = \begin{bmatrix} 1 &amp; 0 &amp; 1 &amp; 0 &amp; 2 \\ 0 &amp; 1 &amp; 0 &amp; 1 &amp; 1 \\ 1 &amp; 1 &amp; 0 &amp; 2 &amp; 0 \end{bmatrix}\)</p> <p>Bias $b_2$ ($3 \times 1$): \(b_2 = \begin{bmatrix} 0 \\ 1 \\ -1 \end{bmatrix}\)</p> <h4 id="linear-transformation-1">Linear Transformation</h4> <p>The transformation is defined as: \(y = W_2 h + b_2\)</p> \[\begin{aligned} W_2 h &amp;= \begin{bmatrix} 1 &amp; 0 &amp; 1 &amp; 0 &amp; 2 \\ 0 &amp; 1 &amp; 0 &amp; 1 &amp; 1 \\ 1 &amp; 1 &amp; 0 &amp; 2 &amp; 0 \end{bmatrix} \begin{bmatrix} 8 \\ 5 \\ 2 \\ 7 \\ 8 \end{bmatrix} \\ &amp;= \begin{bmatrix} 26 \\ 20 \\ 27 \end{bmatrix} \end{aligned}\] <p>Now, add the bias vector $b_2$: \(y = \begin{bmatrix} 26 \\ 20 \\ 27 \end{bmatrix} + \begin{bmatrix} 0 \\ 1 \\ -1 \end{bmatrix} = \begin{bmatrix} 26 \\ 21 \\ 26 \end{bmatrix}\)</p> <h3 id="final-output-of-the-network">Final Output of the Network</h3> \[\boxed{ y = \begin{bmatrix} 26 \\ 21 \\ 26 \end{bmatrix} }\] <p><strong>Shape:</strong> $(3 \times 1)$</p> <h2><br/></h2> <h2 id="loss-function">Loss Function</h2> <p>We use Mean Squared Error (MSE) loss:</p> \[L = \frac{1}{2} \sum_{i=1}^{3} (y_i - t_i)^2\] <p>where $t \in \mathbb{R}^{3}$ is the target vector.</p> <h3 id="gradient-at-the-output-layer">Gradient at the Output Layer</h3> <p>The gradient of the loss with respect to the output is:</p> \[\frac{\partial L}{\partial y_i} = y_i - t_i\] <p>In vector form: \(\delta^{(2)} = \frac{\partial L}{\partial y} = y - t\)</p> <h3 id="gradients-for-the-second-layer">Gradients for the Second Layer</h3> <p>The output layer is: \(y = W_2 h + b_2\)</p> <h4 id="derivative-with-respect-to-a-single-weight">Derivative with Respect to a Single Weight</h4> <p>For a single weight $W_{2_{ij}}$:</p> \[y_i = \sum_{k=1}^{5} W_{2_{ik}} h_k + b_{2_i}\] <p>Applying the chain rule:</p> \[\frac{\partial L}{\partial W_{2_{ij}}} = \frac{\partial L}{\partial y_i} \cdot \frac{\partial y_i}{\partial W_{2_{ij}}} = \delta^{(2)}_i \cdot h_j\] <h4 id="matrix-form">Matrix Form</h4> <p>If we write this for all $i, j$:</p> \[\frac{\partial L}{\partial W_2} = \begin{bmatrix} \delta^{(2)}_1 h_1 &amp; \delta^{(2)}_1 h_2 &amp; \cdots &amp; \delta^{(2)}_1 h_5 \\ \delta^{(2)}_2 h_1 &amp; \delta^{(2)}_2 h_2 &amp; \cdots &amp; \delta^{(2)}_2 h_5 \\ \delta^{(2)}_3 h_1 &amp; \delta^{(2)}_3 h_2 &amp; \cdots &amp; \delta^{(2)}_3 h_5 \end{bmatrix}\] <p>This structure is exactly the definition of the <strong>outer product</strong> between the error vector $\delta^{(2)}$ and the transpose of the hidden vector $h^T$:</p> \[\boxed{ \frac{\partial L}{\partial W_2} = \delta^{(2)} h^T }\] <h4 id="bias-gradient">Bias Gradient</h4> \[\boxed{ \frac{\partial L}{\partial b_2} = \delta^{(2)} }\] <p><br/></p> <h3 id="backpropagating-to-the-hidden-layer">Backpropagating to the Hidden Layer</h3> <p>Each hidden neuron contributes to all output neurons. Using the multivariable chain rule:</p> \[\frac{\partial L}{\partial h_j} = \sum_{i=1}^{3} \frac{\partial L}{\partial y_i} \cdot \frac{\partial y_i}{\partial h_j} = \sum_{i=1}^{3} \delta^{(2)}_i \cdot W_{2_{ij}}\] <p>Stacking all $j$:</p> \[\boxed{ \delta^{(1)} = \frac{\partial L}{\partial h} = W_2^T \delta^{(2)} }\] <h3 id="backpropagation-through-relu">Backpropagation Through ReLU</h3> <p>Since $h = \text{ReLU}(z)$:</p> \[\delta^{(z)} = \frac{\partial L}{\partial z} = \delta^{(1)} \odot \mathbb{I}(z &gt; 0)\] <p>where $\odot$ denotes element-wise multiplication and $\mathbb{I}(z &gt; 0)$ is the indicator function.</p> <h3 id="gradients-for-the-first-layer">Gradients for the First Layer</h3> <p>The first layer is $z = W_1 x + b_1$.</p> <p><strong>Weight Gradient:</strong></p> \[\boxed{ \frac{\partial L}{\partial W_1} = \delta^{(z)} x^T }\] <p><strong>Bias Gradient:</strong></p> \[\boxed{ \frac{\partial L}{\partial b_1} = \delta^{(z)} }\] <h3 id="weight-update-rule">Weight Update Rule</h3> <p>Using gradient descent with learning rate $\eta$:</p> <p>\(W_2 \leftarrow W_2 - \eta \, \delta^{(2)} h^T, \quad b_2 \leftarrow b_2 - \eta \, \delta^{(2)}\) \(W_1 \leftarrow W_1 - \eta \, \delta^{(z)} x^T, \quad b_1 \leftarrow b_1 - \eta \, \delta^{(z)}\)</p> <p><br/></p> <hr/> <h2 id="worked-numerical-example-backward-pass">Worked Numerical Example (Backward Pass)</h2> <p>Let the target be:</p> \[t = \begin{bmatrix} 30 \\ 20 \\ 25 \end{bmatrix}\] <p><strong>Loss:</strong></p> \[L = \frac{1}{2}[(26-30)^2 + (21-20)^2 + (26-25)^2] = \frac{1}{2}[16 + 1 + 1] = 9\] <p><strong>Output Error:</strong></p> \[\delta^{(2)} = y - t = \begin{bmatrix} -4 \\ 1 \\ 1 \end{bmatrix}\] <p><strong>Hidden Layer Error:</strong></p> \[\delta^{(1)} = W_2^T \delta^{(2)} = \begin{bmatrix} -3 \\ 2 \\ -4 \\ 3 \\ -7 \end{bmatrix}\] <p>Since all $z_i &gt; 0$, $\delta^{(z)} = \delta^{(1)}$.</p> <p>With these values, we can now update $W_1, b_1, W_2, b_2$ using the formulas derived above.</p> <p><br/></p> <hr/> <h2 id="the-vanishing-and-exploding-gradient-problem">The Vanishing and Exploding Gradient Problem</h2> <p>In the derivations above, we saw that the gradient for the first layer depends on the product of the weights and error signals from later layers:</p> \[\delta^{(1)} = W_2^T \delta^{(2)}\] <p>In a very deep neural network (many layers), the gradient at an early layer $l$ is calculated by repeatedly multiplying gradients through the Chain Rule:</p> <p>\(\frac{\partial L}{\partial W_l} \propto \underbrace{W_{L}^T \cdot D_{L} \cdot W_{L-1}^T \cdot D_{L-1} \dots}_{\text{Repeated Multiplication}}\) where $D$ represents the diagonal matrix of the derivative of the activation functions.</p> <h3 id="vanishing-gradients">Vanishing Gradients</h3> <p>The vanishing gradient problem occurs when the elements of the chain rule are smaller than 1 (e.g., small weights or activation derivatives $&lt; 1$). As these small numbers are multiplied repeatedly during backpropagation from the output layer to the input layer, the gradient signal shrinks exponentially.</p> <ul> <li><strong>Cause:</strong> Traditional activation functions like <strong>Sigmoid</strong> or <strong>Tanh</strong> have derivatives that are always less than 1 (max 0.25 for Sigmoid).</li> <li><strong>Effect:</strong> The weights in the earlier layers of the network stop updating because their gradients become effectively zero. The network fails to learn features in the early layers.</li> </ul> <h3 id="exploding-gradients">Exploding Gradients</h3> <p>Conversely, if the weights or derivatives are large ($&gt; 1$), repeated multiplication causes the gradients to grow exponentially as they move backward.</p> <ul> <li><strong>Cause:</strong> Poor weight initialization or lack of normalization.</li> <li><strong>Effect:</strong> The weight updates become massive, causing the model parameters to oscillate wildly or diverge to <code class="language-plaintext highlighter-rouge">NaN</code> (Not a Number).</li> </ul> <p><br/></p> <h3 id="why-relu-helps">Why ReLU Helps</h3> <p>In our derivation, we used the <strong>ReLU</strong> activation function. \(\frac{d}{dz} \text{ReLU}(z) = \begin{cases} 1 &amp; \text{if } z &gt; 0 \\ 0 &amp; \text{if } z \le 0 \end{cases}\) Because the derivative is exactly <strong>1</strong> for positive inputs, the gradient signal does not diminish as it passes through active neurons, regardless of network depth. This property makes ReLU (and its variants) essential for training deep networks, effectively solving the vanishing gradient problem for positive activation paths.</p>]]></content><author><name></name></author><category term="Deep Learning"/><category term="deep-learning"/><category term="math"/><category term="neural-networks"/><summary type="html"><![CDATA[A complete mathematical derivation and numerical example of Backpropagation in a two-layer Neural Network.]]></summary></entry><entry><title type="html">Convolutional Neural Network (CNN): Detailed Mechanics and Visualization</title><link href="https://ahmedrafid023.github.io/blog/2026/CNN/" rel="alternate" type="text/html" title="Convolutional Neural Network (CNN): Detailed Mechanics and Visualization"/><published>2026-01-02T00:00:00+00:00</published><updated>2026-01-02T00:00:00+00:00</updated><id>https://ahmedrafid023.github.io/blog/2026/CNN</id><content type="html" xml:base="https://ahmedrafid023.github.io/blog/2026/CNN/"><![CDATA[<p>Convolutional Neural Networks (CNNs) are a class of deep learning models particularly effective for processing grid-like data such as images. This note summarizes the fundamental concepts of CNNs, including the structure of convolutional layers, the role of kernels and filters, multi-channel inputs, and the computation of feature maps through dot products and summations across channels.</p> <p>It also covers how CNNs capture spatial hierarchies in data and the intuition behind pooling and activation functions. Illustrative numeric examples are included to demonstrate the convolution operation and multi-channel processing. This note serves as a practical reference for understanding the inner workings of CNNs and their applications in image recognition and related tasks. <br/> <br/></p> <h2 id="introduction">Introduction</h2> <p>Convolutional Neural Networks (CNNs) are a class of deep neural networks specifically designed to process grid-structured data such as images. They leverage spatial correlations, hierarchical feature learning, and parameter sharing to extract meaningful patterns with high efficiency. CNNs form the backbone of many state-of-the-art systems in image classification, object detection, and more. <br/> <br/></p> <h3 id="input-representation">Input Representation</h3> <p>An image is represented as a three-dimensional tensor:</p> \[\mathbf{X} \in \mathbb{R}^{H \times W \times C}\] <p>where $H$ is the height, $W$ is the width, and $C$ is the number of channels (e.g., $C=1$ for grayscale and $C=3$ for RGB color images).</p> <p>For an RGB image:</p> \[\mathbf{X} = \big[\,\mathbf{X}^{(R)},\,\mathbf{X}^{(G)},\,\mathbf{X}^{(B)}\,\big]\] <p>Each channel represents a stacked matrix capturing different color components.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_images/convnet_fig-480.webp 480w,/assets/img/post_images/convnet_fig-800.webp 800w,/assets/img/post_images/convnet_fig-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_images/convnet_fig.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: Architecture of a Convolutional Neural Network (CNN) illustrating hierarchical feature extraction. A 32x32 input image is processed through successive convolution and max-pooling layers.</figcaption> </figure> <p><br/> <br/></p> <h3 id="convolutional-layer">Convolutional Layer</h3> <p>The convolutional layer is the core computational unit of a CNN. It consists of a set of learnable filters (or kernels), each designed to detect specific features such as edges, textures, and shapes at various spatial locations.</p> <p>A single filter has dimensions:</p> \[\mathbf{W} \in \mathbb{R}^{k \times k \times C}\] <p>with an associated scalar bias $b$. <br/> <br/></p> <h4 id="convolution-operation">Convolution Operation</h4> <p>At a given spatial location $(i,j)$, a convolution computes the inner product between the filter weights and the corresponding local patch of the input across all channels:</p> \[Z_{i,j} = \sum_{c=1}^{C} \sum_{m=0}^{k-1} \sum_{n=0}^{k-1} X^{(c)}_{i+m,j+n} \cdot W^{(c)}_{m,n} + b\] <p><br/></p> <h4 id="channel-summation-intuition">Channel Summation Intuition</h4> <p>A single filter is meant to detect one type of feature across all channels. Each channel provides partial evidence of the feature — for example, an edge might be strong in the red channel and weaker in green or blue. Summing the dot products aggregates this evidence into a single activation value, representing the confidence that the feature exists at that location. <br/> <br/></p> <h3 id="multiple-filters">Multiple Filters</h3> <p>Using $N$ filters produces $N$ output feature maps:</p> \[\text{Output shape} = H' \times W' \times N\] <p>Each filter learns to recognize a distinct feature pattern. <br/> <br/></p> <h3 id="non-linearity-activation-functions">Non-linearity: Activation Functions</h3> <p>After convolution, an element-wise non-linear activation is applied. The Rectified Linear Unit (ReLU) is the most common choice:</p> \[A_{i,j} = \mathrm{ReLU}(Z_{i,j}) = \max(0, Z_{i,j})\] <p>This introduces non-linearity and enables the network to approximate complex functions. <br/> <br/></p> <h3 id="pooling-layer">Pooling Layer</h3> <p>Pooling layers reduce spatial dimensions (height and width) to achieve translation invariance and reduce computation. For a pool size of $p \times p$:</p> \[P_{i,j} = \max_{0 \le m,n &lt; p} A_{i+m,j+n}\] <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_images/pooling-480.webp 480w,/assets/img/post_images/pooling-800.webp 800w,/assets/img/post_images/pooling-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_images/pooling.png" class="img-fluid rounded z-depth-1 mx-auto d-block" width="50%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2: Max pooling operation reducing spatial dimensions.</figcaption> </figure> <p><br/> <br/></p> <h2 id="hierarchical-abstraction">Hierarchical Abstraction</h2> <p>CNNs learn hierarchical representations through successive application of convolution, non-linearity, and pooling. Early layers capture low-level features such as edges and corners. Intermediate layers combine these into motifs like textures, and deeper layers form high-level concepts such as object parts.</p> <p>Interactive visualization techniques—such as those developed by Harley—provide deep insight into these representations. For example, an interactive node visualization platform can be explored at: <a href="https://adamharley.com/nn_vis/cnn/3d.html">https://adamharley.com/nn_vis/cnn/3d.html</a></p> <p>This 3D visualization shows how activations propagate throughout a CNN’s layers when processing an input image.</p> <h2 id="flattening-and-fully-connected-layers">Flattening and Fully Connected Layers</h2> <p>After convolutional and pooling layers, the feature maps are flattened into a vector:</p> \[\mathbf{f} \in \mathbb{R}^{d}\] <p>This vector is passed to one or more fully connected layers:</p> \[\mathbf{h} = \mathbf{W}_{fc} \cdot \mathbf{f} + \mathbf{b}_{fc}\] <p>followed by an activation function.</p> <h2 id="output-layer-and-softmax">Output Layer and Softmax</h2> <p>For classification into $K$ classes, the final layer produces logits:</p> \[\mathbf{o} = \mathbf{W}_{out} \cdot \mathbf{h} + \mathbf{b}_{out}\] <p>The Softmax function converts logits into probabilities:</p> \[\hat{y}_k = \frac{e^{o_k}}{\sum_{j=1}^{K} e^{o_j}}\] <p>with $\sum_{k=1}^{K} \hat{y}_k = 1$.</p> <h2 id="loss-function-and-learning">Loss Function and Learning</h2> <p>The network is trained by minimizing a loss function, such as cross-entropy for classification:</p> \[\mathcal{L} = -\sum_{k=1}^{K} y_k \log(\hat{y}_k)\] <p>where $\mathbf{y}$ is the one-hot ground truth vector.</p> <p>Training uses gradient descent and backpropagation:</p> \[W \leftarrow W - \eta \frac{\partial \mathcal{L}}{\partial W}, \quad b \leftarrow b - \eta \frac{\partial \mathcal{L}}{\partial b}\] <p>where $\eta$ is the learning rate. <br/> <br/></p> <h2 id="interactive-visualization-of-cnn-behavior">Interactive Visualization of CNN Behavior</h2> <p>Interactive visualizations offer educational insights into network dynamics by allowing users to manipulate input and observe layer activations. For example, an interactive node-link visualization designed by Harley shows real-time activations of a CNN trained on handwritten digits, helping students see how input perturbations alter intermediate representations and final predictions. Such tools reveal:</p> <ul> <li>Activation strength and patterns across layers.</li> <li>How filters respond to different input features.</li> <li>The hierarchical transformation from pixels to high-level classification.</li> </ul> <p><br/> <br/></p> <h2 id="summary-of-the-cnn-pipeline">Summary of the CNN Pipeline</h2> \[\text{Input} \rightarrow \text{Convolution} \rightarrow \text{ReLU} \rightarrow \text{Pooling} \rightarrow \text{Flatten} \rightarrow \text{Fully Connected} \rightarrow \text{Softmax}\] <p>This pipeline produces a robust, hierarchical transformation from raw image data to semantic predictions.</p> <hr/> <h2 id="example-walkthroughs">Example Walkthroughs</h2> <h3 id="1-architecture-example-digit-recognition">1. Architecture Example: Digit Recognition</h3> <p><strong>Input:</strong> A $28\times28$ grayscale image of a handwritten digit (e.g., “7”).</p> <ol> <li><strong>Convolutional Layer 1:</strong> 6 filters of size $5\times5$, stride $1$, no padding. <ul> <li>Input shape: $28\times28\times1$</li> <li>Output shape: $24\times24\times6$ (since $28-5+1=24$)</li> </ul> </li> <li><strong>Activation:</strong> ReLU applied → same shape: $24\times24\times6$.</li> <li><strong>Pooling Layer 1:</strong> $2\times2$ max pooling, stride $2$. <ul> <li>Output shape: $12\times12\times6$.</li> </ul> </li> <li><strong>Convolutional Layer 2:</strong> 16 filters of size $5\times5$, stride $1$, no padding. <ul> <li>Input shape: $12\times12\times6$</li> <li>Output shape: $8\times8\times16$.</li> </ul> </li> <li><strong>Activation:</strong> ReLU → $8\times8\times16$.</li> <li><strong>Pooling Layer 2:</strong> $2\times2$ max pooling, stride $2$. <ul> <li>Output shape: $4\times4\times16$.</li> </ul> </li> <li><strong>Flatten:</strong> $4\times4\times16 = 256$ neurons.</li> <li><strong>Fully Connected Layer 1:</strong> 120 neurons.</li> <li><strong>Fully Connected Layer 2:</strong> 84 neurons.</li> <li><strong>Output Layer (Softmax):</strong> 10 neurons (digits $0$–$9$).</li> </ol> <p><strong>Output Probabilities:</strong></p> \[\text{Output probabilities} = \begin{bmatrix} p_0 \\ p_1 \\ p_2 \\ p_3 \\ p_4 \\ p_5 \\ p_6 \\ p_7 \\ p_8 \\ p_9 \end{bmatrix} = \begin{bmatrix} 0.01 \\ 0.02 \\ 0.03 \\ 0.05 \\ 0.04 \\ 0.08 \\ 0.10 \\ \mathbf{0.50} \\ 0.12 \\ 0.05 \end{bmatrix}\] <p>The network predicts class 7 with highest probability ($p_7 = 0.50$).</p> <h3 id="2-calculation-example-2d-convolution">2. Calculation Example: 2D Convolution</h3> <p><strong>Input image:</strong></p> \[\mathbf{X} = \begin{bmatrix} 1 &amp; 2 &amp; 0 &amp; 1 &amp; 0 &amp; 2 \\ 0 &amp; 1 &amp; 2 &amp; 1 &amp; 1 &amp; 0 \\ 1 &amp; 0 &amp; 1 &amp; 2 &amp; 0 &amp; 1 \\ 2 &amp; 1 &amp; 0 &amp; 1 &amp; 1 &amp; 2 \end{bmatrix}\] <p><strong>Kernel (3x3):</strong></p> \[\mathbf{K} = \begin{bmatrix} 1 &amp; 0 &amp; -1 \\ 0 &amp; 1 &amp; 0 \\ -1 &amp; 0 &amp; 1 \end{bmatrix}\] <p><strong>Convolution operation (top-left position):</strong></p> \[\begin{aligned} \text{output}_{1,1} &amp;= (1\cdot1) + (2\cdot0) + (0\cdot(-1)) \\ &amp;+ (0\cdot0) + (1\cdot1) + (2\cdot0) \\ &amp;+ (1\cdot(-1)) + (0\cdot0) + (1\cdot1) \\ &amp;= 2 \end{aligned}\] <p><strong>Next position (top-middle, shift by 1 column):</strong></p> \[\begin{aligned} \text{output}_{1,2} &amp;= (2\cdot1) + (0\cdot0) + (1\cdot(-1)) \\ &amp;+ (1\cdot0) + (2\cdot1) + (1\cdot0) \\ &amp;+ (0\cdot(-1)) + (1\cdot0) + (2\cdot1) \\ &amp;= 5 \end{aligned}\] <p><strong>Output feature map (shape $2\times4$ for stride 1, no padding):</strong></p> \[\mathbf{Y} = \begin{bmatrix} 2 &amp; 5 &amp; ? &amp; ? \\ ? &amp; ? &amp; ? &amp; ? \end{bmatrix}\] <p><br/></p> <h3 id="3-calculation-example-3d-convolution-rgb-input">3. Calculation Example: 3D Convolution (RGB Input)</h3> <p>Instead of a single grid, let’s look at how convolution works on a 3-channel image (like an RGB image).</p> <p><strong>Input Volume ($\mathbf{X}$):</strong> A $3 \times 3$ image with 3 channels ($R, G, B$). Shape: $3 \times 3 \times 3$.</p> \[\mathbf{X}_{R} = \begin{bmatrix} 1 &amp; 0 &amp; 1 \\ 0 &amp; 1 &amp; 0 \\ 1 &amp; 0 &amp; 1 \end{bmatrix} \quad \mathbf{X}_{G} = \begin{bmatrix} 1 &amp; 1 &amp; 0 \\ 1 &amp; 0 &amp; 1 \\ 0 &amp; 1 &amp; 1 \end{bmatrix} \quad \mathbf{X}_{B} = \begin{bmatrix} 0 &amp; 1 &amp; 0 \\ 1 &amp; 1 &amp; 0 \\ 0 &amp; 0 &amp; 2 \end{bmatrix}\] <p><strong>Filter/Kernel ($\mathbf{W}$):</strong> A $2 \times 2$ filter, which also must have 3 channels to match the input depth. Shape: $2 \times 2 \times 3$. Bias $b = 1$.</p> \[\mathbf{W}_{R} = \begin{bmatrix} 1 &amp; -1 \\ 0 &amp; 1 \end{bmatrix} \quad \mathbf{W}_{G} = \begin{bmatrix} 0 &amp; 1 \\ -1 &amp; 1 \end{bmatrix} \quad \mathbf{W}_{B} = \begin{bmatrix} 1 &amp; 0 \\ 0 &amp; -1 \end{bmatrix}\] <p><strong>Convolution Operation (Top-Left Position):</strong></p> <p>To calculate the single output pixel at position $(0,0)$, we calculate the dot product for <em>each</em> channel separately and then sum them all up + bias.</p> <p><strong>Red Channel Contribution:</strong> \((1 \cdot 1) + (0 \cdot -1) + (0 \cdot 0) + (1 \cdot 1) = 1 + 0 + 0 + 1 = 2\)</p> <p><strong>Green Channel Contribution:</strong> \((1 \cdot 0) + (1 \cdot 1) + (1 \cdot -1) + (0 \cdot 1) = 0 + 1 - 1 + 0 = 0\)</p> <p><strong>Blue Channel Contribution:</strong> \((0 \cdot 1) + (1 \cdot 0) + (1 \cdot 0) + (1 \cdot -1) = 0 + 0 + 0 - 1 = -1\)</p> <p><strong>Final Result ($Z_{0,0}$):</strong> \(Z_{0,0} = (\text{Red} + \text{Green} + \text{Blue}) + \text{Bias}\) \(Z_{0,0} = (2 + 0 - 1) + 1 = \mathbf{2}\)</p> <p><strong>Output Feature Map:</strong> Since input is $3 \times 3$ and kernel is $2 \times 2$ (stride 1, no padding), the output will be $2 \times 2$.</p> \[\mathbf{Y} = \begin{bmatrix} \mathbf{2} &amp; ? \\ ? &amp; ? \end{bmatrix}\]]]></content><author><name></name></author><category term="Computer Vision"/><category term="deep-learning"/><category term="computer-vision"/><category term="math"/><summary type="html"><![CDATA[A detailed note on the structure, mechanics, and visualization of CNNs.]]></summary></entry><entry><title type="html">Gradient Descent</title><link href="https://ahmedrafid023.github.io/blog/2026/gradient-descent/" rel="alternate" type="text/html" title="Gradient Descent"/><published>2026-01-02T00:00:00+00:00</published><updated>2026-01-02T00:00:00+00:00</updated><id>https://ahmedrafid023.github.io/blog/2026/gradient-descent</id><content type="html" xml:base="https://ahmedrafid023.github.io/blog/2026/gradient-descent/"><![CDATA[<p>Before we can understand how neural networks learn (via Gradient Descent), we must first understand the “Gradient” itself. In single-variable calculus, the derivative tells us the slope of a line. In deep learning, however, we deal with high-dimensional surfaces. This is the domain of <strong>Multivariable Calculus</strong>.</p> <h2 id="1-the-partial-derivative">1. The Partial Derivative</h2> <p>Imagine a function $f(x, y)$ that depends on two variables. This forms a 3D surface (like a mountain terrain). To find the slope, we cannot just ask “what is the slope?” because the slope depends on which direction we walk.</p> <p>Instead, we ask: <em>“If we hold $y$ constant and only move $x$, how does $f$ change?”</em></p> <p>This is the <strong>partial derivative</strong> with respect to $x$, denoted as:</p> \[\frac{\partial f}{\partial x}\] <p>It treats all other variables as constants and measures the sensitivity of the function to just one variable.</p> <p><br/></p> <h2 id="2-the-gradient-vector-nabla">2. The Gradient Vector ($\nabla$)</h2> <p>The <strong>Gradient</strong> is simply a vector that collects all these partial derivatives together. It is denoted by the symbol nabla ($\nabla$).</p> <p>For a function $f: \mathbb{R}^n \rightarrow \mathbb{R}$ with inputs $\mathbf{x} = [x_1, x_2, \dots, x_n]$, the gradient is:</p> \[\nabla f(\mathbf{x}) = \begin{bmatrix} \frac{\partial f}{\partial x_1} \\ \frac{\partial f}{\partial x_2} \\ \vdots \\ \frac{\partial f}{\partial x_n} \end{bmatrix}\] <p>Even though $f$ outputs a single scalar value (like “Loss”), the gradient $\nabla f$ is a <strong>vector</strong> that lives in the input space.</p> <p><br/> <br/></p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <div id="gradient-plot" style="width: 100%; height: 500px;"></div> <script src="https://cdn.plot.ly/plotly-latest.min.js"></script> <script>
        fetch("/assets/json/gradient_surface.json")
            .then(response => response.json())
            .then(data => {
            Plotly.newPlot('gradient-plot', data.data, data.layout);
            });
        </script> </div> </div> <div class="caption"> Figure 1: An interactive 3D "Loss Landscape". The Gradient vector at any point on this surface points in the direction of steepest ascent (uphill). </div> <p><br/> <br/></p> <h2 id="3-geometric-interpretation-the-key-insight">3. Geometric Interpretation (The Key Insight)</h2> <p>This is the most important concept for Machine Learning:</p> <blockquote> <p><strong>The Gradient vector $\nabla f(\mathbf{x})$ points in the direction of the steepest ascent.</strong></p> </blockquote> <p>If you are standing on a mountain side (a point on the Loss landscape):</p> <ol> <li>The <strong>magnitude</strong> (length) of the vector tells you how steep the slope is.</li> <li>The <strong>direction</strong> of the vector points to the peak (uphill).</li> </ol> <p>Because we want to minimize error (go to the bottom of the valley), we must move in the <strong>opposite</strong> direction of the gradient. This is why the update rule has a minus sign.</p> <hr/> <p><br/></p> <h2 id="4-analyzing-the-plot-a-concrete-example">4. Analyzing the Plot: A Concrete Example</h2> <p>Let’s look at the “Bowl” shape plotted above. Mathematically, this surface represents a simple convex function:</p> \[J(x, y) = x^2 + y^2\] <p>Imagine we initialize our weights at the point <strong>$(3, 4)$</strong>. At this position, our “Loss” (height) is $3^2 + 4^2 = 25$. We are high up on the hill.</p> <h3 id="step-1-calculate-the-gradient">Step 1: Calculate the Gradient</h3> <p>To find the slope, we compute the partial derivatives:</p> \[\frac{\partial J}{\partial x} = 2x, \quad \frac{\partial J}{\partial y} = 2y\] <p>Evaluating at our point $(3, 4)$:</p> \[\nabla J(3, 4) = \begin{bmatrix} 2(3) \\ 2(4) \end{bmatrix} = \begin{bmatrix} 6 \\ 8 \end{bmatrix}\] <h3 id="step-2-what-does-this-vector-6-8-mean">Step 2: What does this vector $[6, 8]$ mean?</h3> <ol> <li><strong>Direction:</strong> The vector points in the direction of positive $x$ and positive $y$. If you look at the plot, moving “outward” (away from the center) goes <strong>uphill</strong>. The gradient always points to the peak.</li> <li><strong>Magnitude:</strong> The steepness is $\sqrt{6^2 + 8^2} = 10$. This is a steep slope!</li> </ol> <p><br/></p> <h2 id="5-performing-gradient-descent">5. Performing Gradient Descent</h2> <p>Since we want to minimize loss (go downhill), we must move in the <strong>opposite</strong> direction of the gradient.</p> <p>Let’s apply the update rule with a learning rate (step size) of $\alpha = 0.1$:</p> \[\begin{aligned} \begin{bmatrix} x_{new} \\ y_{new} \end{bmatrix} &amp;= \begin{bmatrix} x_{old} \\ y_{old} \end{bmatrix} - \alpha \cdot \nabla J(3, 4) \\ &amp;= \begin{bmatrix} 3 \\ 4 \end{bmatrix} - 0.1 \begin{bmatrix} 6 \\ 8 \end{bmatrix} \\ &amp;= \begin{bmatrix} 3 - 0.6 \\ 4 - 0.8 \end{bmatrix} \\ &amp;= \begin{bmatrix} 2.4 \\ 3.2 \end{bmatrix} \end{aligned}\] <h3 id="the-result">The Result</h3> <p>We moved from $(3, 4)$ to $(2.4, 3.2)$. Did we improve? Let’s check the new Loss: \(J(2.4, 3.2) = 2.4^2 + 3.2^2 = 5.76 + 10.24 = 16\) The Loss decreased from <strong>25</strong> to <strong>16</strong>. We have successfully taken one step towards the bottom of the bowl.</p> <hr/> <p><br/></p> <h2 id="6-how-it-works-in-deep-learning-backpropagation">6. How it works in Deep Learning (Backpropagation)</h2> <p>In the example above, we calculated derivatives by hand because the function $x^2 + y^2$ is simple. However, a Deep Neural Network is a massive composite function with millions of parameters:</p> \[\hat{y} = \sigma(W_3 \cdot \sigma(W_2 \cdot \sigma(W_1 \cdot x)))\] <p>We cannot derive a simple formula for the gradient of this monster. Instead, we use the <strong>Chain Rule</strong>. This process is called <strong>Backpropagation</strong>.</p> <h3 id="the-chain-rule-visualized">The Chain Rule Visualized</h3> <p>Imagine a simple computational graph where input $x$ passes through a weight $w$ and bias $b$ to calculate a Loss $L$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <pre style="line-height: 1.5; font-family: monospace; background: transparent; border: 1px solid rgba(127, 127, 127, 0.2); padding: 15px; border-radius: 5px;">
      [Input x] 
           \
            ( * ) ────&gt; [ z = w·x ] ────&gt; ( Loss L ) &lt;──── [Target y]
           /
      [Weight w]

FORWARD:  Move Left to Right (Calculate Loss)
BACKWARD: Move Right to Left (Calculate Gradients)
</pre> </div> </div> <div class="caption"> Figure 2: Text-based computational graph. Inputs x and w combine to form z, which is compared against target y to compute Loss. </div> <p>Mathematically, if $y = f(u)$ and $u = g(x)$, then the change in $y$ caused by $x$ is the product of the individual changes:</p> \[\frac{\partial y}{\partial x} = \frac{\partial y}{\partial u} \cdot \frac{\partial u}{\partial x}\] <h3 id="deep-learning-example">Deep Learning Example</h3> <p>Let’s trace a single neuron with a squared error loss.</p> <ol> <li><strong>Forward Pass:</strong> We compute the prediction and the error. <ul> <li>$z = w \cdot x$</li> <li>$L = (z - y_{true})^2$</li> </ul> </li> <li> <p><strong>Backward Pass (Calculating Gradient):</strong> We want to know: <em>How does changing the weight $w$ affect the Loss $L$?</em></p> <p>We start from the Loss and work backward:</p> </li> </ol> \[\frac{\partial L}{\partial w} = \underbrace{\frac{\partial L}{\partial z}}_{\text{Step 1: Loss to Output}} \cdot \underbrace{\frac{\partial z}{\partial w}}_{\text{Step 2: Output to Weight}}\] <p>Let’s calculate the pieces:</p> <ul> <li>$\frac{\partial L}{\partial z} = 2(z - y_{true})$</li> <li>$\frac{\partial z}{\partial w} = x$</li> </ul> <p>So the gradient for weight $w$ is:</p> \[\nabla w = 2(w \cdot x - y_{true}) \cdot x\] <p>In frameworks like PyTorch or TensorFlow, this “backward” multiplication happens automatically. It starts at the Loss function and propagates the error signal back through every layer, computing the gradient for every single parameter in the network efficiently.</p>]]></content><author><name></name></author><category term="Deep Learning"/><category term="math"/><category term="calculus"/><category term="deeplearning"/><summary type="html"><![CDATA[Understanding the Gradient vector, Partial Derivatives, and Backpropagation.]]></summary></entry><entry><title type="html">Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra</title><link href="https://ahmedrafid023.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/" rel="alternate" type="text/html" title="Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra"/><published>2024-05-14T00:00:00+00:00</published><updated>2024-05-14T00:00:00+00:00</updated><id>https://ahmedrafid023.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra</id><content type="html" xml:base="https://ahmedrafid023.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/"><![CDATA[<p>Learn more:Learn more:Learn more:Learn more:Learn more:Learn more:May 14, 2024 We’re introducing a series of updates across the Gemini family of models, including the new 1.5 Flash, our lightweight model for speed and efficiency, and Project Astra, our vision for the future of AI assistants. In December, we launched our first natively multimodal model Gemini 1.0 in three sizes: Ultra, Pro and Nano. Just a few months later we released 1.5 Pro, with enhanced performance and a breakthrough long context window of 1 million tokens.Developers and enterprise customers have been putting 1.5 Pro to use in incredible ways and finding its long context window, multimodal reasoning capabilities and impressive overall performance incredibly useful.We know from user feedback that some applications need lower latency and a lower cost to serve. This inspired us to keep innovating, so today, we’re introducing Gemini 1.5 Flash: a model that’s lighter-weight than 1.5 Pro, and designed to be fast and efficient to serve at scale.Both 1.5 Pro and 1.5 Flash are available in public preview with a 1 million token context window in Google AI Studio and Vertex AI. And now, 1.5 Pro is also available with a 2 million token context window via waitlist to developers using the API and to Google Cloud customers.We’re also introducing updates across the Gemini family of models, announcing our next generation of open models, Gemma 2, and sharing progress on the future of AI assistants, with Project Astra.Context lengths of leading foundation models compared with Gemini 1.5’s 2 million token capability1.5 Flash is the newest addition to the Gemini model family and the fastest Gemini model served in the API. It’s optimized for high-volume, high-frequency tasks at scale, is more cost-efficient to serve and features our breakthrough long context window.While it’s a lighter weight model than 1.5 Pro, it’s highly capable of multimodal reasoning across vast amounts of information and delivers impressive quality for its size.The new Gemini 1.5 Flash model is optimized for speed and efficiency, is highly capable of multimodal reasoning and features our breakthrough long context window.1.5 Flash excels at summarization, chat applications, image and video captioning, data extraction from long documents and tables, and more. This is because it’s been trained by 1.5 Pro through a process called “distillation,” where the most essential knowledge and skills from a larger model are transferred to a smaller, more efficient model.Read more about 1.5 Flash in our updated Gemini 1.5 technical report, on the Gemini technology page, and learn about 1.5 Flash’s availability and pricing.Over the last few months, we’ve significantly improved 1.5 Pro, our best model for general performance across a wide range of tasks.Beyond extending its context window to 2 million tokens, we’ve enhanced its code generation, logical reasoning and planning, multi-turn conversation, and audio and image understanding through data and algorithmic advances. We see strong improvements on public and internal benchmarks for each of these tasks.1.5 Pro can now follow increasingly complex and nuanced instructions, including ones that specify product-level behavior involving role, format and style. We’ve improved control over the model’s responses for specific use cases, like crafting the persona and response style of a chat agent or automating workflows through multiple function calls. And we’ve enabled users to steer model behavior by setting system instructions.We added audio understanding in the Gemini API and Google AI Studio, so 1.5 Pro can now reason across image and audio for videos uploaded in Google AI Studio. And we’re now integrating 1.5 Pro into Google products, including Gemini Advanced and in Workspace apps.Read more about 1.5 Pro in our updated Gemini 1.5 technical report and on the Gemini technology page.Gemini Nano is expanding beyond text-only inputs to include images as well. Starting with Pixel, applications using Gemini Nano with Multimodality will be able to understand the world the way people do — not just through text, but also through sight, sound and spoken language.Read more about Gemini 1.0 Nano on Android.Today, we’re also sharing a series of updates to Gemma, our family of open models built from the same research and technology used to create the Gemini models.We’re announcing Gemma 2, our next generation of open models for responsible AI innovation. Gemma 2 has a new architecture designed for breakthrough performance and efficiency, and will be available in new sizes.The Gemma family is also expanding with PaliGemma, our first vision-language model inspired by PaLI-3. And we’ve upgraded our Responsible Generative AI Toolkit with LLM Comparator for evaluating the quality of model responses.Read more on the Developer blog.As part of Google DeepMind’s mission to build AI responsibly to benefit humanity, we’ve always wanted to develop universal AI agents that can be helpful in everyday life. That’s why today, we’re sharing our progress in building the future of AI assistants with Project Astra (advanced seeing and talking responsive agent).To be truly useful, an agent needs to understand and respond to the complex and dynamic world just like people do — and take in and remember what it sees and hears to understand context and take action. It also needs to be proactive, teachable and personal, so users can talk to it naturally and without lag or delay.While we’ve made incredible progress developing AI systems that can understand multimodal information, getting response time down to something conversational is a difficult engineering challenge. Over the past few years, we’ve been working to improve how our models perceive, reason and converse to make the pace and quality of interaction feel more natural.Building on Gemini, we’ve developed prototype agents that can process information faster by continuously encoding video frames, combining the video and speech input into a timeline of events, and caching this information for efficient recall.By leveraging our leading speech models, we also enhanced how they sound, giving the agents a wider range of intonations. These agents can better understand the context they’re being used in, and respond quickly, in conversation.With technology like this, it’s easy to envision a future where people could have an expert AI assistant by their side, through a phone or glasses. And some of these capabilities are coming to Google products, like the Gemini app and web experience, later this year.We’ve made incredible progress so far with our family of Gemini models, and we’re always striving to advance the state-of-the-art even further. By investing in a relentless production line of innovation, we’re able to explore new ideas at the frontier, while also unlocking the possibility of new and exciting Gemini use cases.Learn more about Gemini and its capabilities. Your information will be used in accordance with Google’s privacy policy.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      Done. Just one step more.
    
      Check your inbox to confirm your subscription.
    You are already subscribed to our newsletter.
    You can also subscribe with a
    different email address
    
    .
    
  Let’s stay in touch. Get the latest news from Google in your inbox.
          Follow Us
</code></pre></div></div>]]></content><author><name></name></author><summary type="html"><![CDATA[We’re sharing updates across our Gemini family of models and a glimpse of Project Astra, our vision for the future of AI assistants.]]></summary></entry></feed>