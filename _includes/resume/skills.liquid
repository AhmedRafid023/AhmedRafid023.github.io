<ul class="skills-list">
  {% for content in data[1] %}
    <li>
      <b>{{ content.name }}:</b>
      <span class="skill-items">{{ content.keywords | join: ", " }}</span>
    </li>
  {% endfor %}
</ul>
